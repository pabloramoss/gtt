/** @type {import('next').NextConfig} */
const withPWA = require('next-pwa')({
  dest: 'public',
  register: true,
  skipWaiting: true,
});

const nextConfig = withPWA({
  // Podés agregar más configuraciones si necesitás
});

module.exports = nextConfig;
